# RoachRace Data Package

Data models and structures for RoachRace game.

## Purpose

This package contains pure data models and structures that can be shared across different systems and packages without dependencies on UI or other systems.

## Usage

Add your data models to the `Runtime` folder. All classes will be part of the `RoachRace.Data` namespace.
